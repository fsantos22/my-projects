export type classBodyType = {
  name: string;
  period: string;
  startDate: string;
  endDate: string;
  module: number;
};

export enum classBody {
  name = "name",
  period = "period",
  startDate = "startDate",
  endDate = "endDate",
  module = "module",
} 